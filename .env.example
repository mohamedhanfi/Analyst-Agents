# Copy to .env and fill in your keys. .env is never committed (.gitignore).
OPENROUTER_API_KEY=sk-or-v1-...
# Optional: override the default model from config.yaml
# CREW_MODEL=deepseek/deepseek-v4-flash